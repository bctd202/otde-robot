import type { ProviderStatus as Provider } from '../types';
import { formatEasternTime } from '../lib/dates';
export function ProviderStatus({provider,updated,refreshing,onRefresh}:{provider:Provider;updated:Date|null;refreshing:boolean;onRefresh:()=>void}) { const mode=provider.mode==='live'&&provider.delay_seconds===0?'LIVE':provider.mode==='delayed'||provider.delay_seconds>0?'DELAYED':provider.mode==='mock'||provider.provider==='mock'?'MOCK':'UNKNOWN'; return <div className="provider-status" aria-label="Market data status"><span>{provider.provider.toUpperCase()}</span><strong>{mode}</strong><span>Last updated {formatEasternTime(updated)}</span><span className={`refreshing-indicator ${refreshing?'visible':''}`} aria-live="polite">Refreshing…</span><button type="button" onClick={onRefresh} disabled={refreshing}>Refresh</button></div>; }
